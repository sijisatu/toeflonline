PORT=4000
APP_ENV=development
APP_ORIGIN=https://toeflonline-production.up.railway.app,http://localhost:4173
BACKEND_TLS_ENABLED=false
JWT_ACCESS_SECRET=replace-with-strong-secret
JWT_REFRESH_SECRET=replace-with-strong-secret

# PostgreSQL — copy actual values from Railway dashboard (Postgres service → Variables)
POSTGRES_HOST=${{ Postgres.PGHOST }}
POSTGRES_PORT=${{ Postgres.PGPORT }}
POSTGRES_DB=${{ Postgres.PGDATABASE }}
POSTGRES_USER=${{ Postgres.PGUSER }}
POSTGRES_PASSWORD=${{ Postgres.PGPASSWORD }}

# Redis — fill in if a Redis service is provisioned on Railway
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

PROCTORING_SNAPSHOT_PATH=storage/proctoring
